## This repo contains the ansible playbooks
**Command to run the ansible playbooks on the CLI**
```agsl
ansible-playbook -i inventory.ini -e ansible_user=ec2-user -e  ansible_password=DevOps321 01-playbook.yaml
```
